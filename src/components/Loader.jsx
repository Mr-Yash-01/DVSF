import React from 'react' // Import the React library
import '../index.css' // Import the CSS file for styling

// Define the Loader functional component
const Loader = () => {
    return (
        // Return a div with a class name 'loader' and height of 10 units
        <div className='loader h-10'> {/* Add the class name here */}
            {/* Content of the loader */}
        </div>
    )
}

// Export the Loader component as the default export
export default Loader